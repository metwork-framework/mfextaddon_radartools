{% extends "layer.md" %}

{% block overview %}

The radartools_system_libraries layer contains a few copies (at build) of centos8 boost system libraries needed as runtime dependencies but that we don't want to build from sources

{% endblock %}

